<%= form_with(model: space, local: true) do |form| %>
  <% if space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(space.errors.count, "error") %> prohibited this space from being saved:</h2>

      <ul>
        <% space.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :airtable_id %>
    <%= form.text_field :airtable_id %>
  </div>

  <div class="field">
    <%= form.label :space_id %>
    <%= form.text_field :space_id %>
  </div>

  <div class="field">
    <%= form.label :site_id %>
    <%= form.text_field :site_id %>
  </div>

  <div class="field">
    <%= form.label :space_name %>
    <%= form.text_field :space_name %>
  </div>

  <div class="field">
    <%= form.label :space_description %>
    <%= form.text_field :space_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
